Use the following command to debug:

    # sudo munin-run <plugin_name>
    # sudo munin-run <plugin_name> autoconf
    # sudo munin-run <plugin_name> config

Note: you have to create soft links in /etc/munin/plugins

