Use the following command to debug:

    # munin-run <plugin_name>
    # munin-run <plugin_name> autoconf
    # munin-run <plugin_name> config

Note: you have to make soft links to /etc/munin/plugins

